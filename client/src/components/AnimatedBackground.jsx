// O fundo "seda" agora é aplicado direto no body (ver index.css) — à prova de
// z-index e de WebGL. Este componente fica vazio para não duplicar camadas.
export default function AnimatedBackground() {
  return null;
}
